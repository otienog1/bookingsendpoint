[tool:pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts =
    --tb=short
    --strict-markers
    --disable-warnings
    --quiet
markers =
    unit: Unit tests
    integration: Integration tests
    slow: Slow tests